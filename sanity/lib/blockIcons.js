import { AlignCenter, AlignJustify, AlignLeft, AlignRight } from "lucide-react";

export const Center = () => {
  return <AlignCenter size={15} />;
};

export const Left = () => {
  return <AlignLeft size={15} />;
};

export const Right = () => {
  return <AlignRight size={15} />;
};

export const Justify = () => {
  return <AlignJustify size={15} />;
};
